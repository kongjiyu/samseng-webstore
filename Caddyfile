{$SAMSENG_SITE_URL} {
    handle_path /uploads/* {
        root * /var/www/uploads
        file_server

        header Content-Disposition attachment
    }

    reverse_proxy {$SAMSENG_SERVER} {
    }
}